---
layout: layouts/base.njk
---

<article class="max-w-screen-md mx-auto" role="article" aria-labelledby="{{ title | slug }}-title">
  <header>
    <h1 class="mb-5" id="{{ title | slug }}-title">
      {% if date %}<span class="font-mono font-normal">{{ date | date }}</span>{% endif %}
      <span>{{ title }}</span>
    </h1>
  </header>
  
  {{ content | safe }}
</article>
